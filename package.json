{
  "name": "logistics-intel-monorepo",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:frontend": "vitest run --root frontend",
    "test:edge": "deno test --allow-env --allow-net=esm.sh,deno.land supabase/functions/_shared/"
  },
  "devDependencies": {
    "vitest": "^4.0.12"
  }
}
