import React from "react";

/**
 * NOTE:
 * This file previously contained an unscoped top-level `useEffect(...)` snippet,
 * which breaks eslint's hooks rules. The functional Company page implementation
 * lives elsewhere in the app.
 */
export default function Company() {
  return null;
}
